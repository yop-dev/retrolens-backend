# Production Environment Configuration for Shared Backend
# This configuration allows local frontend development with deployed backend

# Supabase Configuration (Required - Get from Supabase Dashboard)
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_KEY=your_supabase_service_key_here  
SUPABASE_PROJECT_ID=your_supabase_project_id_here

# Security (Required - Generate secure key for production!)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GENERATE_A_SECURE_RANDOM_KEY_HERE
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=RetroLens
VERSION=1.0.0
DEBUG=False

# Server Configuration  
HOST=0.0.0.0
PORT=8000

# CORS Origins - IMPORTANT: Allows local development
# Add your deployed frontend URL when ready
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:4173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Clerk Configuration (Required for authentication)
CLERK_DOMAIN=your-clerk-domain.clerk.accounts.dev
CLERK_SECRET_KEY=sk_live_your_clerk_secret_key_here
CLERK_AUDIENCE=your_clerk_audience_here
